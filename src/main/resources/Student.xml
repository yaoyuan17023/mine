<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 为DAO接口方法提供sql语句配置 -->
<mapper namespace="HeMapper">
    <resultMap id="Student" type="Student">
        <result property="name" column="name"/>
        <result property="qq" column="qq"/>
        <result property="type" column="type"/>
        <result property="time" column="time"/>
        <result property="school" column="school"/>
        <result property="number" column="number"/>
        <result property="link" column="link"/>
        <result property="hope" column="hope"/>
        <result property="referrer" column="referrer"/>
        <result property="from" column="from"/>
        <result property="create_at" column="create_at"/>
        <result property="update_at" column="update_at"/>
    </resultMap>
    <!-- 查询 -->
    <select id="selectStudent" parameterType="int" resultType="Student">
        SELECT * FROM student WHERE id = #{id}
    </select>
    <select id="getStudentList" parameterType="String" resultMap="Student">
        select * from student where name = #{name}
    </select>
    <!-- 增加 -->
    <insert id="addStudent" parameterType="Student" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student(name,qq,type,time,school,number,link,hope,referrer,from,create_at)
        values(#{name},#{qq},#{type},#{time},#{school},#{number},#{link},#{hope},#{referrer},#{from},#{create_at})
        <selectKey keyProperty="ID" order="AFTER" resultType="Integer">
            select last_insert_id()
        </selectKey>
    </insert>
    <!-- 修改 -->
    <update id="updateStudent" parameterType="Student">
        UPDATE student set name=#{name},qq=#{qq},type=#{type},time=#{time},school=#{school},number=#{number},link=#{link},hope=#{hope},
        referrer=#{referrer},from=#{from},create_at=#{create_at},update_at=#{update_at} WHERE id = #{id}
    </update>
    <!-- 删除 -->
    <delete id="deleteStudent" parameterType="int">
        DELETE FROM student WHERE id = #{id}
    </delete>
</mapper>